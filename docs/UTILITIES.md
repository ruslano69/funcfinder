# funcfinder Utilities Guide

funcfinder поставляется с двумя дополнительными утилитами для анализа кода: **stat** и **deps**.

## 📦 Установка

```bash
# Собрать все утилиты одной командой
./build.sh

# Или собрать по отдельности
go build -o funcfinder
go build -o stat stat.go
go build -o deps deps.go
```

---

## 🔍 stat - Function Call Counter

Анализирует вызовы функций в исходных файлах. Помогает найти "горячие точки" в коде.

### Синтаксис

```bash
stat [OPTIONS] <source_file>
  -l <lang>   Force language (py, go, rs, js, sw, c, java, d, cs)
  -n <num>    Show top N functions
```

### Поддерживаемые языки

Python, Go, Rust, JavaScript/TypeScript, Swift, C/C++, Java, D, C#

### Примеры использования

#### Базовое использование

```bash
# Анализ Go файла (язык определяется автоматически)
stat finder.go

# Output:
# Language: Go
# Functions: 28
# -----------------------------------
# append                    11
# len                       5
# CountBraces               4
# ...
```

#### Топ-N самых вызываемых функций

```bash
# Показать только 10 самых вызываемых
stat main.go -n 10

# Топ-20 в Python файле
stat api.py -n 20
```

#### Принудительный выбор языка

```bash
# Для файлов с нестандартным расширением
stat script.txt -l py -n 15
stat config.inc -l c -n 10
```

---

## 📊 deps - Dependency Analyzer

Анализирует зависимости модулей в проекте. Показывает граф импортов и классифицирует их.

### Синтаксис

```bash
deps [OPTIONS] <dir>
  -l <lang>   Force language
  -n <num>    Show top N dependencies
  -j, --json  Output JSON
```

### Поддерживаемые языки

Python, Go, Rust, JavaScript/TypeScript, Java, C#, Swift, C/C++

### Классификация зависимостей

- **stdlib** - стандартная библиотека языка
- **external** - внешние пакеты (npm, pip, cargo, etc.)
- **internal** - внутренние модули проекта (vendor/, internal/)

### Примеры использования

#### Анализ текущего проекта

```bash
# Анализ Go проекта в текущей директории
deps . -l go

# Output:
# Language: Go
# Total imports: 12
# Unique modules: 12
# -----------------------------------
# stdlib: 6, external: 1, internal: 5
# -----------------------------------
# fmt                             11 (std)
# strings                         10 (std)
# os                               7 (std)
# ...
```

#### Топ-N зависимостей

```bash
# Показать только топ-10 самых используемых
deps . -l go -n 10

# Топ-20 для JavaScript проекта
deps ./src -l js -n 20
```

#### JSON вывод

```bash
# Для интеграции с CI/CD или визуализации
deps . -l go -j > dependencies.json

# Output:
# {
#   "language": "Go",
#   "total_imports": 12,
#   "unique_modules": 12,
#   "dependencies": [
#     {
#       "module": "fmt",
#       "count": 11,
#       "files": ["main.go", "finder.go", ...]
#     },
#     ...
#   ],
#   "external_vs_internal": {
#     "stdlib": 6,
#     "external": 1,
#     "internal": 5
#   }
# }
```

---

## 🎯 Типичные сценарии использования

### Сценарий 1: Первичный анализ незнакомого кода

**Цель:** Быстро понять структуру и зависимости нового проекта

```bash
# Шаг 1: Посмотреть общую структуру
funcfinder --inp main.go --source go --map

# Шаг 2: Найти самые вызываемые функции (вероятные entry points)
stat main.go -l go -n 15

# Шаг 3: Изучить граф зависимостей
deps . -l go -j | jq '.dependencies[] | select(.count > 5)'
```

**Результат:** За 30 секунд получаете карту кодовой базы: структуру, горячие точки, ключевые зависимости.

---

### Сценарий 2: Рефакторинг - поиск дублирования

**Цель:** Найти часто вызываемые функции для возможной оптимизации

```bash
# Найти функции, вызываемые >20 раз
stat service.go -l go | awk '$2 > 20 {print $1}'

# Извлечь тела этих функций для анализа
funcfinder --inp service.go --source go --func validateInput,processData --extract
```

**Результат:** Список кандидатов на вынос в утилиты или оптимизацию.

---

### Сценарий 3: Анализ зависимостей перед миграцией

**Цель:** Понять какие модули нужно обновить/заменить

```bash
# Получить все внешние зависимости
deps . -l py -j | jq '.dependencies[] | select(.module | contains("."))'

# Найти самые используемые stdlib модули
deps . -l go -n 20 | grep '(std)'

# Проверить internal модули
deps . -l go -j | jq '.dependencies[] | select(.module | contains("internal"))'
```

**Результат:** Четкая картина зависимостей для планирования миграции.

---

### Сценарий 4: Code Review - анализ изменений

**Цель:** Быстро понять влияние изменений в PR

```bash
# Получить список функций в новом коде
funcfinder --inp new_feature.go --source go --map > new_funcs.txt

# Посмотреть какие функции вызываются
stat new_feature.go -l go -n 10

# Проверить новые зависимости
deps . -l go -j > after_deps.json
# Сравнить с before_deps.json
```

**Результат:** Понимание scope изменений за минуту.

---

### Сценарий 5: AI-агент навигация

**Цель:** Минимизировать токены для AI при работе с большими файлами

```bash
# 1. AI получает карту файла (100 токенов вместо 10000)
funcfinder --inp large_service.go --source go --map --json

# 2. AI выбирает интересующие функции из карты
# 3. Извлекаются только нужные функции (300 токенов вместо 10000)
funcfinder --inp large_service.go --source go \
  --func ProcessOrder,ValidatePayment --extract

# 4. Бонус: AI видит где функция используется
stat large_service.go -l go | grep "ProcessOrder"

# 5. Бонус: AI видит зависимости модуля
deps . -l go -j | jq '.dependencies[] | select(.count > 3)'
```

**Результат:** 95%+ экономия токенов при сохранении контекста.

---

### Сценарий 6: Поиск узких мест (bottlenecks)

**Цель:** Найти функции, которые могут быть узкими местами

```bash
# Найти функции с большим количеством вызовов
stat server.go -l go -n 5

# Output:
# validateRequest          45
# logMetrics              38
# checkAuth               32
# parseJSON               28
# handleError             25

# Извлечь эти функции для профилирования
funcfinder --inp server.go --source go \
  --func validateRequest,logMetrics,checkAuth --extract
```

**Результат:** Список функций-кандидатов для оптимизации с приоритетом.

---

### Сценарий 7: Документирование архитектуры

**Цель:** Автоматически создать документацию по структуре проекта

```bash
# Получить дерево классов и методов
funcfinder --inp api.go --source go --tree > structure.txt

# Получить статистику по вызовам
for file in *.go; do
  echo "=== $file ===" >> calls.txt
  stat $file -l go -n 10 >> calls.txt
done

# Получить граф зависимостей
deps . -l go -j > dependencies.json

# Визуализация через jq
deps . -l go -j | jq -r '.dependencies[] | "\(.module) -> \(.count)"'
```

**Результат:** Автоматически сгенерированная документация архитектуры.

---

## 💡 Лучшие практики

### Best Practice 1: Комбинируйте утилиты

Используйте все три утилиты вместе для полной картины:

```bash
#!/bin/bash
# analyze.sh - скрипт полного анализа

FILE=$1
LANG=$2

echo "=== Structure ==="
funcfinder --inp $FILE --source $LANG --tree

echo -e "\n=== Hot Functions ==="
stat $FILE -l $LANG -n 10

echo -e "\n=== Dependencies ==="
deps . -l $LANG -n 10
```

### Best Practice 2: Автоматизируйте через CI/CD

```yaml
# .github/workflows/code-analysis.yml
- name: Analyze dependencies
  run: |
    ./deps . -l go -j > deps-report.json
    # Проверить лимит внешних зависимостей
    EXTERNAL=$(jq '.external_vs_internal.external' deps-report.json)
    if [ $EXTERNAL -gt 20 ]; then
      echo "Too many external dependencies: $EXTERNAL"
      exit 1
    fi
```

### Best Practice 3: Используйте для code review

```bash
# Перед review
git diff --name-only origin/main | grep '\.go$' | while read file; do
  echo "=== $file ==="
  funcfinder --inp $file --source go --map
  stat $file -l go -n 5
done
```

### Best Practice 4: Фильтруйте результаты через jq

```bash
# Только external зависимости с count > 5
deps . -l go -j | jq '.dependencies[] |
  select(.count > 5) |
  select(.module | contains("/"))'

# stdlib зависимости, сортировка по использованию
deps . -l go -j | jq '.dependencies[] |
  select(.module | test("^(fmt|os|strings)")) |
  {module, count}' | jq -s 'sort_by(.count) | reverse'
```

### Best Practice 5: Создавайте отчеты

```bash
# weekly-report.sh
DATE=$(date +%Y-%m-%d)
mkdir -p reports/$DATE

# Структура кода
for file in *.go; do
  funcfinder --inp $file --source go --tree >> reports/$DATE/structure.txt
done

# Топ-функции
stat *.go -l go > reports/$DATE/hotspots.txt

# Зависимости
deps . -l go -j > reports/$DATE/dependencies.json

# Сравнить с прошлой неделей
diff reports/last-week/dependencies.json reports/$DATE/dependencies.json
```

---

## 🔧 Технические детали

### Как работает stat

1. Читает файл построчно
2. Удаляет комментарии и строковые литералы
3. Находит паттерн `функция(` через regex
4. Подсчитывает каждое вхождение
5. Исключает ключевые слова (if, for, while, etc.)
6. Сортирует по убыванию

### Как работает deps

1. Сканирует директорию рекурсивно
2. Находит import/use/require паттерны
3. Классифицирует модули (stdlib/external/internal)
4. Подсчитывает использование в разных файлах
5. Возвращает aggregated статистику

### Производительность

- **stat**: ~1-2ms на 1000 строк кода
- **deps**: ~10-20ms на 100 файлов
- **Memory**: Minimal, streaming обработка

---

## 🤝 Интеграция с другими инструментами

### С grep

```bash
# Найти файлы с TODO, затем проанализировать
grep -l "TODO" *.go | while read file; do
  stat $file -l go -n 3
done
```

### С git

```bash
# Анализ изменений в последнем коммите
git diff-tree --no-commit-id --name-only -r HEAD |
  grep '\.go$' |
  xargs -I {} funcfinder --inp {} --source go --map
```

### С jq (JSON обработка)

```bash
# Сложные запросы к deps
deps . -l go -j | jq '
  .dependencies[] |
  select(.count > 3) |
  {name: .module, usage: .count, files: .files | length}
'
```

---

## 📚 Примеры для разных языков

### Python

```bash
stat script.py -l py -n 10
deps . -l py -j > python-deps.json
funcfinder --inp api.py --source py --tree
```

### JavaScript/TypeScript

```bash
stat app.js -l js -n 15
deps ./src -l js -j
funcfinder --inp index.ts --source ts --tree-full
```

### Rust

```bash
stat main.rs -l rs -n 10
deps . -l rs -j | jq '.dependencies[] | select(.module | startswith("std::"))'
```

### C/C++

```bash
stat server.cpp -l c -n 20
deps . -l c -n 10
```

---

## ❓ FAQ

**Q: Почему stat показывает `if` в списке функций?**
A: Используйте конфигурацию языка - `if` должен быть в ExcludeWords. Проверьте `-l` флаг.

**Q: deps не находит мои internal модули**
A: deps ищет паттерны `internal/` и `vendor/`. Убедитесь в правильной структуре.

**Q: Можно ли расширить языки?**
A: Да, stat.go и deps.go используют config-driven подход. Добавьте новый language config.

**Q: JSON формат для автоматизации?**
A: Только deps поддерживает `-j`. Для stat используйте парсинг через awk/grep.

---

## 🔗 См. также

- [README.md](README.md) - основная документация funcfinder
- [CHANGELOG.md](CHANGELOG.md) - история изменений
